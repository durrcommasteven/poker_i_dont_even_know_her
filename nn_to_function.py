import numpy as np 
import tensorflow as tf 
def save_weights(sess, vars):
	#this saves the weights as arrays
	
def tf_nn_to_function(sess, vars, standard = True):
	#given the vars for a particlar neural network 
	#this returns a function which does what the neural network does